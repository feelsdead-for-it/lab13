#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	int  a, b, f, n;

	cin >> n;
	while (n <= 2)
	{
	
		cin >> n;
	}
	 cin >> a;
	cin >> b;
	f = a+b;
	int* m = new int[n];
	m[0] = a;
	m[1] = b;
	for (int i = 2; i < n; i++)
	{
		
    	m[i] = f;
		f = f + f;
	}

	for (int i = 0; i < n; i++)
	{
		cout << m[i] << "\t";
	}


}
